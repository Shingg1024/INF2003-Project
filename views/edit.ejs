<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('include/_header') %>

<%- include('include/_nav') %>
  <!-- Add your other head elements here -->
  <style>

    .navbar {
      border-radius: 0;
      /* Add other styles as needed */
    }
    body {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      margin: 0;
      background: linear-gradient(to bottom, #f0f0f0, #d8d8d8);
    }

    main {
      flex: 1;
    }

    footer {
      padding: 15px;
      color: #fff;
      text-align: center;
      background-color: rgb(16, 15, 15);
      backdrop-filter: blur(5px);
      margin-top: auto;
    }


    .card {
      border: none; /* Remove card border */
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Add a subtle box shadow */
      
    }

    .card-header h3 {
      margin-bottom: 0;
    }

    .form-control {
      padding: auto;
    }
  </style>
</head>
<body>


<div class="container">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card">
          <div class="card-header">
            <h3>Edit</h3>
          </div>
          <div class="card-body">
            <form method="post" action="/edit">
                        <div class="mb-3">
                            <label for="email" class="form-label">Email</label>
                            <input type="email" class="form-control" id="email" name="email" required value="<%= session.user.email %>">
                        </div>
                        <div class="mb-3">
                            <label for="password" class="form-label">Password</label>
                            <input type="password" class="form-control" id="password" name="password" required>
                        </div>
                        <div class="mb-3">
                            <label for="first_name" class="form-label">First Name</label>
                            <input type="text" class="form-control" id="first_name" name="first_name" required value="<%= session.user.first_name %>">
                        </div>
                        <div class="mb-3">
                            <label for="last_name" class="form-label">Last Name</label>
                            <input type="text" class="form-control" id="last_name" name="last_name" required value="<%=session.user.last_name %>">
                        </div>
                        <div class="mb-3">
                            <label for="biography" class="form-label">Biography</label>
                            <textarea class="form-control" id="biography" name="biography" rows="3"><%= session.user.profile && session.user.profile.biography ? session.user.profile.biography : '' %></textarea>
                        </div>
                        <div class="mb-3">
                            <label for="interests" class="form-label">Interests</label>
                            <input type="text" class="form-control" id="interests" name="interests" value="<%= session.user.profile && session.user.profile.interests ? session.user.profile.interests : '' %>">
                        </div>
                        <div class="mb-3">
                            <label for="twitter" class="form-label">Twitter</label>
                            <input type="text" class="form-control" id="twitter" name="twitter" value="<%= session.user.profile && session.user.profile.socialMedia && session.user.profile.socialMedia.twitter ? session.user.profile.socialMedia.twitter : '' %>">
                        </div>
                        <div class="mb-3">
                            <label for="instagram" class="form-label">Instagram</label>
                            <input type="text" class="form-control" id="instagram" name="instagram" value="<%= session.user.profile && session.user.profile.socialMedia && session.user.profile.socialMedia.instagram ? session.user.profile.socialMedia.instagram : '' %>">
                        </div>
                        <div class="mb-3">
                            <label for="facebook" class="form-label">Facebook</label>
                            <input type="text" class="form-control" id="facebook" name="facebook" value="<%= session.user.profile && session.user.profile.socialMedia && session.user.profile.socialMedia.facebook ? session.user.profile.socialMedia.facebook : '' %>">
                        </div>
                        <button type="submit" class="btn btn-primary">Edit Profile</button>
                    </form>
                </div>
              </div>
            </div>
          </div>
        </div>

        <footer>
            <p>&copy; 2023 Your Company. All rights reserved.</p>
          </footer>
          
          </body>
          </html>